<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>CvManager.Tools.DemoRoster</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Only for the shared dataset model/loader; no runtime project references this tool. -->
    <ProjectReference Include="..\..\api\Infrastructure\CvManager.Infrastructure.csproj" />
  </ItemGroup>

</Project>
